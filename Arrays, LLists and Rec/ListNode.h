template <typename T>
class Listnode
{
private:
	Listnode<T>* next; //pointer to next item in the list
	//Listnode* next; //pointer to next item in the list
	T elem;

	/*public:
		Listnode(T val ,  nxt):
		elem{val} , nxt = nullptr {}*/
};