#include <iostream>

//Pointers
/*
A pointer is a special variable that holds an address of a variable
Given a type, T, the type T* denotes a pointer to a variable of type T
Pointer operators:
& - address of operator: returns address of an object in memory
* - deference operator: accessing object's value at the address

*/


//Pointers and arrays
/*
NAME of ARRAY = pointer to array's initial element 
char c[] = {'c' , 'd' , 'e'};
char *p = c;			//p points to c[0]
char *q = &c[0];		//q ALSO points to c[0]

*/

//Pointers dynamic memory and the "new" operator
/*
operator ~new~  

*/